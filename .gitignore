# deps / logs / os junk
node_modules/
*.log
server.log
.DS_Store

# Large raw data (>50MB) — committed as .gz instead. gunzip to use.
obseleted/old-extraction-pipeline/leicht_items.json
obseleted/old-extraction-pipeline/leicht_catalog.json
docs/export-v767.json
docs/export-v781.json
# local pre-change safety backups of the full export — never committed (each ~95–103MB)
docs/*.bak.json

# Finder duplicate — not shipped at all (no .gz either)
obseleted/duplicate-data/

# v2 fresh full exports (raw >50MB) — committed as .gz instead
docs/export-v781-fresh.json
docs/export-v781-min.json
